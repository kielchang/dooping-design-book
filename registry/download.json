{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "download",
  "version": "0.13.0",
  "type": "registry:lib",
  "title": "download 下載工具",
  "description": "觸發瀏覽器下載 Blob。",
  "dependencies": [
    "@dooping/tokens@^0.7.0"
  ],
  "registryDependencies": [],
  "files": [
    {
      "path": "dooping/lib/download.ts",
      "content": "export function saveBlob(blob: Blob, fileName: string): void {\n  // 觸發瀏覽器下載一個 Blob。無框架相依，供 CSV／檔案匯出共用。\n  // 說明寫在函式內而不是檔頭：shadcn CLI 安裝時會刪掉檔案開頭的註解（tests/registry-content.test.ts）。\n  const url = URL.createObjectURL(blob);\n  const a = document.createElement(\"a\");\n  a.href = url;\n  a.download = fileName;\n  document.body.appendChild(a);\n  a.click();\n  a.remove();\n  URL.revokeObjectURL(url);\n}\n",
      "type": "registry:lib",
      "target": "lib/dooping/download.ts"
    }
  ]
}
